buildscript {
    ext {
        corda_release_group   = 'net.corda'
        corda_core_release_group = 'net.corda'
        corda_release_version = '4.11'
        corda_core_release_version = '4.11'
        corda_gradle_plugins_version = '5.0.12'
        kotlin_version = '1.2.71'
        junit_version = '4.12'
        quasar_version = '0.7.12_r3'
        corda_platform_version = '11'
    }
    repositories {
        mavenCentral()
        maven { url 'https://download.corda.net/maven/corda-releases' }
        maven { url 'https://download.corda.net/maven/corda-dependencies' }
        maven { url 'https://repo.gradle.org/gradle/libs-releases' }
    }
    dependencies {
        classpath "net.corda.plugins:cordapp:$corda_gradle_plugins_version"
        classpath "net.corda.plugins:cordformation:$corda_gradle_plugins_version"
        classpath "net.corda.plugins:quasar-utils:$corda_gradle_plugins_version"
    }
}

apply plugin: 'net.corda.plugins.cordapp'
apply plugin: 'net.corda.plugins.cordformation'

allprojects {
    repositories {
        mavenCentral()
        maven { url 'https://download.corda.net/maven/corda-releases' }
        maven { url 'https://download.corda.net/maven/corda-dependencies' }
        maven { url 'https://repo.gradle.org/gradle/libs-releases' }
    }
}

dependencies {
    cordaCompile "$corda_release_group:corda-core:$corda_release_version"
    cordaRuntime "$corda_release_group:corda:$corda_release_version"
    cordaRuntime "$corda_release_group:corda-node-api:$corda_release_version"
    cordaRuntime "$corda_release_group:corda-node:$corda_release_version"

    cordapp project(':contracts-java')
    cordapp project(':workflows-java')
}

task deployNodes(type: net.corda.plugins.Cordform, dependsOn: ['contracts-java:jar', 'workflows-java:jar']) {
    nodeDefaults {
        projectCordapp {
            deploy = false
        }
        cordapp project(':contracts-java')
        cordapp project(':workflows-java')
        rpcUsers = [[ user: "user1", password: "test", permissions: ["ALL"]]]
    }

    node {
        name "O=Notary,L=London,C=GB"
        notary = [validating: false]
        p2pPort 10002
        rpcSettings {
            address("localhost:10003")
            adminAddress("localhost:10023")
        }
    }
    node {
        name "O=Garvit,L=New Delhi,C=IN"
        p2pPort 10005
        rpcSettings {
            address("localhost:10006")
            adminAddress("localhost:10026")
        }
    }
    node {
        name "O=Arnav,L=Mumbai,C=IN"
        p2pPort 10008
        rpcSettings {
            address("localhost:10009")
            adminAddress("localhost:10029")
        }
    }
    node {
        name "O=Mridul,L=Bengaluru,C=IN"
        p2pPort 10011
        rpcSettings {
            address("localhost:10012")
            adminAddress("localhost:10032")
        }
    }
}
